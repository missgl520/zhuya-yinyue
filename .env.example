# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 竹笌后端环境变量示例（复制为 .env 后填写）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 【必填】Agnes API Key。留空则后端进入「mock 演示模式」，App 仍可完整对话。
# 获取地址参考前端代码：https://apihub.agnes-ai.cn（国内版）
AGNES_API_KEY=
# 是否使用 Agnes 国内版（true/false）
AGNES_USE_CN=true
# 模型名（一般无需改）
AGNES_MODEL=agnes-2.0-flash

# 服务监听端口
PORT=8000

# ── ACE Music（音乐狗子真实音乐生成）──
# 留空 → 音乐生成为 Mock 占位模式（App 流程可用，但不出真实音频）。
# 填入「已轮换」的新密钥即启用真实生成：POST https://api.acemusic.ai/v1/chat/completions
# ⚠️ 安全红线：旧密钥 c2fa5ed9b9124ebf8a61a093629b8727 已泄漏，必须去服务商
#    轮换后填此处，切勿写回任何代码文件（git 历史也别留）。
ACE_MUSIC_API_KEY=

# 【可选】LiveKit 语音通话。不填则 /livekit/connect 返回 available:false
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# 默认情感角色（gentle/playful/wise）
PERSONA_DEFAULT=gentle
# 默认唤醒词
WAKE_WORD_DEFAULT=竹笌竹笌

# ── 接口签名鉴权（API Key + 请求签名）──
# 【生产必填】强随机值；留空则开发模式不强制签名（仅限本地联调）。
# 前端须通过 --dart-define=ZHUYU_API_KEY=<相同值> 同步配置。
ZHUYU_API_KEY=zhuyu-dev-key-change-me
# 签名时间容差（秒），防重放，建议保持 300 以内
SIGNATURE_TOLERANCE_SECONDS=300
# CORS 允许的源（逗号分隔，生产必填具体域名）。留空则仅放行本地来源（不使用 *）。
# 示例：ALLOWED_ORIGINS=https://app.example.com,https://example.com

# ── 静态加密（at-rest encryption）──
# 个人数据（记忆 content、好感度 JSON）落盘加密密钥。
# 取值：base64 编码的 32 字节 Fernet key（用 python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())" 生成）。
# 留空则首次运行自动在 data/.enc_key 生成并持久化（gitignore）。生产建议显式设置。
# ZHUYU_ENC_KEY=
